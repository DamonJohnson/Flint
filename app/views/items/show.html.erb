
<div class="px-4 py-5 my-5 text-center">
    <img class="d-block mx-auto mb-4" src="app/assets/images/hero.png" alt="" width="100vw">
    <h1 class="display-5 fw-bold"><%= @item.title %></h1>
    <div class="col-lg-6 mx-auto">
      <p class="lead mb-4"><%= @item.description %></p>
      <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
      <% if @item.user_id != current_user.id %>
        <button type="button" class="btn btn-primary btn-lg px-4 gap-3" >Hire</button>
      <% end %>
      </div>
    </div>
  </div>

<%# Use the check url method that was used for show profile %>
<% if @item.user_id == current_user.id %>
<p>This is your item. It is currently available to hire for other users.</p>
  <div class="btn-toolbar">
<<<<<<< HEAD
    <%= link_to('Edit', edit_item_path(@item), class: 'btn btn-warning me-2') %>
    <%= button_to('Delete', @item, class: 'btn btn-danger', method: :delete, form: { data: { turbo_confirm: "Are you sure you want to delete '#{@item.title}' ?" } }) %>
  </div>
=======
    <%= link_to('Edit Item', edit_item_path(@item), class: 'btn btn-warning me-2') %> 
    <%= button_to('Remove', @item, class: 'btn btn-danger', method: :delete, form: { data: { turbo_confirm: "Are you sure you want to delete '#{@item.title}' ?" } }) %>
  </div>
  <% end %>
>>>>>>> master
